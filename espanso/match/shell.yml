matches:
  - trigger: ":dot"
    replace: "{{output}}"
    vars:
      - name: output
        type: shell
        params:
            cmd: "echo cd /Users/elalitte/dotfiles/"
  - trigger: ":vim"
    replace: "{{output}}"
    vars:
      - name: output
        type: shell
        params:
            cmd: "echo cd /Users/elalitte/dotfiles/nvim/lua/elalitte/"
